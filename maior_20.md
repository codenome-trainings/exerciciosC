//exerciciosC
//Digitar um número, e imprimir se for maior que 20.

#include <stdio.h>

int main() {
	int x;
	printf("Digite um número: ");
	scanf("%d", &x);
	if(x > 20) {
		printf("%d\n", x);
	}
	
}
